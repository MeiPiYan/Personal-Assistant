# Personal Assistant 向量检索与知识库改造方案

> 文档版本：v1.0
> 编写日期：2026-09-17
> 适用项目：Personal Assistant（Windows 桌面端个人 AI 助手，Python + PySide6）
> 文档性质：架构演进方案 / 可行性分析

---

## 目录

1. [背景与目标](#1-背景与目标)
2. [现状诊断](#2-现状诊断)
3. [可行性结论](#3-可行性结论)
4. [目标总体架构](#4-目标总体架构)
5. [详细设计](#5-详细设计)
   - 5.1 [存储层：sqlite-vec](#51-存储层sqlite-vec)
   - 5.2 [Embedding 模型层](#52-embedding-模型层)
   - 5.3 [结构化分块](#53-结构化分块)
   - 5.4 [混合检索](#54-混合检索)
   - 5.5 [RAG 接入对话](#55-rag-接入对话)
6. [数据模型设计](#6-数据模型设计)
7. [接口设计](#7-接口设计)
8. [分阶段落地路线](#8-分阶段落地路线)
9. [风险与应对](#9-风险与应对)
10. [依赖清单变更](#10-依赖清单变更)
11. [测试与验收标准](#11-测试与验收标准)
12. [附录：关键代码位置索引](#12-附录关键代码位置索引)

---

## 1. 背景与目标

### 1.1 背景

当前 Personal Assistant 在「文本读取 + 知识存储」环节采用的是**纯关键词路线**：

- 文档读取阶段把各类文件压成**无结构纯文本**；
- 存储阶段依赖 **SQLite FTS5（BM25）+ LIKE 兜底**；
- 全项目**没有任何语义理解 / 向量检索能力**。

这导致知识库本质上只是一个「加了索引的文本仓库」，而非能支撑智能问答的知识资产。

### 1.2 目标

1. **文本读取**：从「纯文本榨取」升级为「结构感知解析 + 元数据保留」。
2. **知识存储**：从「关键词倒排索引」升级为「向量语义索引 + 关键词索引」双通道。
3. **语义检索**：支持同义词、近义表述、跨语言改写等多种自然语言检索。
4. **知识库能力**：使知识库可作为 RAG（检索增强生成）的知识源，直接服务于对话。
5. **最低风险落地**：最大化复用现有 SQLite / aiosqlite / AIEngine 架构，渐进式改造。

---

## 2. 现状诊断

### 2.1 逐层现状与短板

| 环节 | 关键文件 | 现状 | 关键短板 |
|---|---|---|---|
| 解析 | `src/document/parser.py` | PDF / DOCX / TXT / MD / HTML → 纯文本 | 丢失标题层级、页码、表格结构；`.doc` 实际不被 `python-docx` 支持；中文 TXT 固定 `utf-8`，GBK 文件会解析失败 |
| 分块 | `src/document/chunker.py` | 按 `字符数 // 3` 近似 token，`chunk_size=3000`、`overlap=200`，在句号处切分 | 无段落/标题感知；3000 token 对向量检索**粒度过粗**，语义被稀释 |
| 摘要 | `src/document/summarizer.py` | 分层摘要（分块摘要 → 合并），纯 prompt 生成 | 摘要生成后即丢弃，未沉淀为可检索资产 |
| 存储 | `src/storage/database.py` | SQLite + aiosqlite；6 张业务表 + 3 张 FTS5 虚拟表 | 无向量表；无 chunk 级切片存储 |
| 数据访问 | `src/storage/dao.py` | `insert_*` 同步写 FTS；`search_*` 走 FTS5 `MATCH` | 检索能力受限于关键词 |
| 检索 | `src/search/local_search.py` | FTS5 `MATCH` → 失败回退 `LIKE` | 中文 `unicode61` 不分词，实际退化为子串匹配；**同义词、近义表述完全检索不到** |
| AI | `src/ai/models.py` | 8 家供应商，**仅 chat 模型** | 无 embedding 能力，需新增模型类型 |
| UI | `src/ui/knowledge_panel.py` | 手工录入 + FTS 搜索 | 无向量检索入口、无相关度排序 |

### 2.2 空白确认

全项目检索 `embed / vector / faiss / chroma / numpy / llama_index / langchain` **零命中**，`pyproject.toml` 依赖中也无任何向量库或 embedding 框架。

> **核心痛点一句话**：现在只能「搜到原词」，搜不到「意思相近的内容」。

---

## 3. 可行性结论

**结论：可行，且价值明确。**

理由：

1. **架构兼容度高**：项目已基于 SQLite + aiosqlite，向量扩展（`sqlite-vec`）可直接挂载到现有连接上，无需引入独立服务。
2. **AI 层可复用**：现有 8 供应商配置体系（openai / deepseek / siliconflow / anthropic / gemini / groq / ollama / custom）可自然扩展出 embedding 后端。
3. **增量可落地**：可分阶段实施，P0 阶段即可用最低风险跑通闭环。
4. **收益直接**：语义检索 + RAG 是个人知识助手从「工具」升级为「助手」的关键跃迁。

---

## 4. 目标总体架构

```
                         用户查询
                            │
              ┌─────────────┴─────────────┐
              ▼                           ▼
      ┌───────────────┐          ┌───────────────┐
      │  BM25 (FTS5)  │          │  向量检索      │
      │  现有 capability│          │  (sqlite-vec)  │
      └───────┬───────┘          └───────┬───────┘
              │  Top-N                    │  Top-N
              └─────────────┬─────────────┘
                            ▼
                  ┌───────────────────┐
                  │  RRF 融合重排      │
                  └─────────┬─────────┘
                            ▼
                  ┌───────────────────┐
                  │  Top-K 上下文      │
                  └─────────┬─────────┘
                            ▼
                  ┌───────────────────┐
                  │  AIEngine.chat()   │  ← 生成答案 (RAG)
                  └───────────────────┘
```

**数据写入流：**

```
文档/知识录入
   → DocumentParser 解析（保留元数据）
   → TextChunker 结构化分块
   → EmbeddingBackend 生成向量
   → 写入 chunks 表 (sqlite) + vec_chunks 虚拟表 (sqlite-vec)
   → 同步写入 FTS5 表 (保留关键词检索)
```

---

## 5. 详细设计

### 5.1 存储层：sqlite-vec

**选型对比：**

| 方案 | 优点 | 缺点 | 适配度 |
|---|---|---|---|
| **sqlite-vec** ✅ | SQLite 扩展、单文件、可直接挂到现有 aiosqlite 连接 | 生态较新 | ★★★★★ |
| ChromaDB | 自带持久化 + 元数据过滤、上手快 | 引入独立存储层、较重 | ★★★★ |
| LanceDB | 列存、查询快、支持过滤 | 与现有 SQLite 双存储 | ★★★★ |
| FAISS | 性能强 | 无持久化/元数据、需自行管理 | ★★★ |
| Qdrant / Milvus | 功能全 | 需独立服务，过重 | ★★ |

**推荐：`sqlite-vec`**，与现有 `Database` 单例无缝共存，迁移成本最低。

**关键实现点：**

- 通过 `sqlite_vec.load(conn)` 加载扩展；
- `aiosqlite` 需使用 `enable_load_extension(True)`（注意 Android/iOS 等平台可能默认禁用）；
- 向量表使用 `vec0` 虚拟表，声明维度与距离度量（推荐 `cosine` 或 `L2`）；
- 维度需与所选 embedding 模型输出一致（如 bge-small-zh-v1.5 为 512 维，bge-m3 为 1024 维）。

### 5.2 Embedding 模型层

**推荐方案（三选一或做成可切换）：**

| 方案 | 模型 | 维度 | 特点 |
|---|---|---|---|
| 本地-轻量 ✅ | `BAAI/bge-small-zh-v1.5` | 512 | CPU 可跑、体积小，最贴合个人助手 |
| 本地-高质量 | `BAAI/bge-m3` | 1024 | 中英混合、质量高，但体积大、CPU 慢 |
| 云端 | SiliconFlow / Ollama embedding | 视模型 | 复用现有供应商，无需下载，依赖网络 |

**设计建议：**

- 抽象出 `EmbeddingBackend` 接口，与现有 8 供应商配置风格保持一致；
- 支持配置项 `ai.embedding.provider` / `ai.embedding.model` / `ai.embedding.dim`；
- 输出向量必须**归一化**（若使用 cosine 距离）；
- **异步阻塞是最大陷阱**：本地 embedding 是 CPU 密集操作，会卡住 `qasync` 事件循环，**必须放入线程池**（`asyncio.to_thread` / `run_in_executor`），否则 UI 假死。

### 5.3 结构化分块

这是**检索效果的上限**，必须改造。

**现状问题：** `chunker.py` 按 `字符数 // 3` 近似 token，chunk_size=3000 过大。

**改造目标：**

1. **结构感知**：
   - Markdown：按标题层级（`#`/`##`/`###`）切分，保留章节路径；
   - PDF：按段落 + 页码切分；
   - DOCX：按标题样式 + 段落；
   - 纯文本：按空行段落 + 句子边界。
2. **粒度调整**：向量粒度建议 **300–500 token**（远小于当前 3000）；检索命中后再做相邻 chunk 上下文扩展。
3. **元数据保留**：每个 chunk 携带 `doc_id / chunk_index / page / section / char_start / char_end`。
4. **重叠**：保持 10%–20% 重叠避免语义截断。

### 5.4 混合检索

**不要用向量完全替换 FTS5**——中文专有名词、数字、代码片段上 BM25 反而更准，二者互补。

```
并行执行：
  ├─ BM25 (FTS5 MATCH)  → 关键词 Top-N
  └─ 向量检索 (vec0)      → 语义 Top-N
        │
        ▼
  RRF (Reciprocal Rank Fusion) 融合
        score = Σ 1 / (k + rank_i)   (k 通常取 60)
        │
        ▼
  可选的 Rerank 模型二次精排（如 bge-reranker）
        │
        ▼
  Top-K 结果
```

### 5.5 RAG 接入对话

在 `ChatPanel` 发送消息前：

1. 用混合检索召回相关 chunk；
2. 拼装进 system prompt（附带来源标注）；
3. 调用现有 `AIEngine.chat()`（接口无需改动）；
4. UI 上展示引用来源，支持点击跳转原文。

---

## 6. 数据模型设计

### 6.1 新增表

```sql
-- 文档主表（一次入库对应一条）
CREATE TABLE IF NOT EXISTS documents (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    title         TEXT,
    source_path   TEXT,              -- 文件路径或 URL
    source_type   TEXT,              -- pdf / docx / md / txt / html / manual
    content_hash  TEXT UNIQUE,       -- 用于去重（sha256）
    meta          TEXT,              -- JSON：作者、页数、字数、创建时间等
    created_at    TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 分块表（向量检索的最小单元）
CREATE TABLE IF NOT EXISTS chunks (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    doc_id        INTEGER NOT NULL REFERENCES documents(id) ON DELETE CASCADE,
    chunk_index   INTEGER NOT NULL,  -- 文档内序号
    content       TEXT NOT NULL,
    token_count   INTEGER,
    page          INTEGER,           -- PDF 页码等
    section       TEXT,              -- 所属章节路径
    char_start    INTEGER,
    char_end      INTEGER,
    created_at    TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX IF NOT EXISTS idx_chunks_doc ON chunks(doc_id);

-- 向量虚拟表（sqlite-vec）
CREATE VIRTUAL TABLE IF NOT EXISTS vec_chunks USING vec0(
    chunk_id  INTEGER PRIMARY KEY,
    embedding FLOAT[512]             -- 维度随模型调整
);

-- 分块全文索引（保留关键词通道）
CREATE VIRTUAL TABLE IF NOT EXISTS chunks_fts USING fts5(
    content,
    content='chunks',
    content_rowid='id'
);
```

### 6.2 写入一致性（触发器或 DAO 内同步）

由于安全考虑，推荐在 **DAO 层显式同步**（与现有 `insert_message` 等风格一致），而非 SQLite 触发器（触发器不易维护、难调试）：

- 写入 `documents` → 写入 `chunks`（逐块）→ 写入 `vec_chunks`（逐块向量）→ 写入 `chunks_fts`。

> ⚠️ **历史教训提醒**：本项目曾有「FTS5 虚拟表建了但从不写数据，导致搜索永远为空」的问题。因此**任何新增虚拟表（`vec_chunks`、`chunks_fts`）都必须在写入路径同步填充**，并加测试覆盖。

### 6.3 现有表处置

- `knowledge_items`：保留，作为「手工知识条目」入口；新增知识时同步生成 chunk + 向量。
- `messages_fts / diary_fts / knowledge_fts`：保留，继续作为关键词通道。
- 新增 `chunks / vec_chunks / documents`：承载语义通道。

---

## 7. 接口设计

### 7.1 Embedding 抽象

```python
# src/ai/embedding.py（新增）
from abc import ABC, abstractmethod

class EmbeddingBackend(ABC):
    @abstractmethod
    async def embed(self, texts: list[str]) -> list[list[float]]:
        """批量生成向量，返回归一化后的 float 列表。"""
        ...

    @property
    @abstractmethod
    def dim(self) -> int:
        ...
```

实现：`LocalBgeBackend`（sentence-transformers）、`SiliconFlowEmbeddingBackend`、`OllamaEmbeddingBackend`。

### 7.2 检索抽象

```python
# src/search/vector_search.py（新增）
class VectorSearcher:
    def __init__(self, db, backend: EmbeddingBackend):
        ...

    async def index_chunks(self, doc_id: int, chunks: list[str]) -> None:
        """生成向量并写入 vec_chunks。"""

    async def search(self, query: str, top_k: int = 10) -> list[dict]:
        """向量检索，返回 [{chunk_id, content, score, doc_id}, ...]。"""

# src/search/hybrid_search.py（新增）
class HybridSearcher:
    """BM25 + 向量 + RRF 融合。"""
    def __init__(self, local_searcher, vector_searcher):
        ...

    async def search(self, query: str, top_k: int = 10, k: int = 60) -> list[dict]:
        ...
```

### 7.3 现有接口扩展

- `DAO`：新增 `insert_document` / `insert_chunks` / `search_hybrid`。
- `DocumentParser`：返回值由 `str` 升级为 `ParsedDocument(text, meta, sections)`。
- `ChatPanel`：发送前调用 `HybridSearcher.search()` 注入上下文。

---

## 8. 分阶段落地路线

| 阶段 | 内容 | 交付 | 风险 |
|---|---|---|---|
| **P0** | 接入 `sqlite-vec` + 建 `chunks`/`vec_chunks` 表 + `EmbeddingBackend` 封装，**先只对新入库知识生效** | 新数据可语义检索 | 低 |
| **P1** | 结构化分块改造（`Parser`/`Chunker`）+ 存量数据回填脚本（批量 embedding，含限流/重试/进度） | 全量数据向量化 | 中 |
| **P2** | 混合检索（BM25+向量+RRF）+ RAG 注入对话 + UI 来源展示 | 端到端 RAG | 中 |

**建议**：P0 先跑通最小闭环，验证 embedding 质量与性能后，再投入 P1/P2。

---

## 9. 风险与应对

| # | 风险 | 影响 | 应对 |
|---|---|---|---|
| 1 | **异步事件循环阻塞** | UI 假死 | 本地 embedding 放入线程池 `asyncio.to_thread` |
| 2 | **存量数据无向量** | 老数据搜不到 | P1 编写回填脚本，失败重试 + 进度反馈 |
| 3 | **存储体积膨胀** | DB 显著增大 | 评估量化压缩（如 float16 / int8）；或对低频数据按需向量化 |
| 4 | **本地模型体积/延迟** | CPU 推理慢 | 首选 `bge-small-zh-v1.5`；或改用云端 embedding |
| 5 | **云端 embedding 成本** | 额外费用 | 批量 + 缓存（content_hash 去重，避免重复向量化） |
| 6 | **维度不匹配** | 写入/查询报错 | 维度作为配置项，模型变更需重建索引 |
| 7 | **虚拟表不同步** | 检索为空 | 写入路径显式同步 + 单元测试覆盖（吸取历史教训） |
| 8 | **平台不支持扩展加载** | 无法加载 sqlite-vec | 启动时检测 `enable_load_extension`；失败则降级到纯 FTS5 |

---

## 10. 依赖清单变更

在 `pyproject.toml` 中新增：

```toml
# Vector Search & Embedding
"sqlite-vec>=0.1",                 # SQLite 向量扩展
"sentence-transformers>=3.0",      # 本地 embedding（若用本地模型）
"numpy>=1.26",                     # 向量运算
# 可选：若走本地高质量模型
# "torch>=2.2",                    # sentence-transformers 依赖（体积大）
```

> 说明：若选择**云端 embedding**（SiliconFlow / Ollama），可去掉 `sentence-transformers` 与 `torch`，仅保留 `sqlite-vec` 与 `numpy`，显著减小安装体积。

> 同时建议清理当前 `pyproject.toml` 中已确认**未被使用**的依赖（如 `litellm`），避免依赖膨胀。

---

## 11. 测试与验收标准

### 11.1 单元测试

- `tests/test_embedding.py`：`EmbeddingBackend` 输出维度、归一化、批量一致性。
- `tests/test_vector_search.py`：
  - 写入 chunk → `vec_chunks` 有数据；
  - 语义查询（同义改写）能召回目标 chunk；
  - 删除文档级联删除 chunk 与向量。
- `tests/test_hybrid_search.py`：RRF 融合排序正确、BM25/向量结果均被纳入。

### 11.2 验收标准

1. 语义检索：查询「如何提升模型回答质量」能召回含「prompt engineering」的 chunk。
2. 关键词检索：专有名词（如「DeepSeek」）结果不劣于改造前。
3. 性能：单次语义查询（本地模型）< 2s，且 UI 不卡顿。
4. 一致性：任意写入路径后，`chunks`、`vec_chunks`、`chunks_fts` 三者行数对齐。
5. 降级：`sqlite-vec` 加载失败时，系统仍可用纯 FTS5 正常检索。

---

## 12. 附录：关键代码位置索引

| 功能 | 文件 | 改造动作 |
|---|---|---|
| 文档解析 | `src/document/parser.py` | 返回结构化 `ParsedDocument`，保留元数据 |
| 分块 | `src/document/chunker.py` | 改为结构感知 + 300–500 token |
| 摘要 | `src/document/summarizer.py` | 摘要结果沉淀入库 |
| 数据库 | `src/storage/database.py` | 加 `documents`/`chunks`/`vec_chunks`/`chunks_fts` |
| 数据访问 | `src/storage/dao.py` | 加文档/分块写入与同步、混合检索入口 |
| 本地检索 | `src/search/local_search.py` | 保留 BM25 通道 |
| 向量检索 | `src/search/vector_search.py`（新增） | 向量检索实现 |
| 混合检索 | `src/search/hybrid_search.py`（新增） | RRF 融合 |
| Embedding | `src/ai/embedding.py`（新增） | 抽象 + 各后端实现 |
| AI 引擎 | `src/ai/models.py` | 扩展 embedding 模型类型 |
| 知识面板 | `src/ui/knowledge_panel.py` | 接入混合检索 + 来源展示 |
| 对话面板 | `src/ui/chat_panel.py` | RAG 上下文注入 |

---

## 结语

本方案推荐走 **「sqlite-vec + 结构化分块 + BM25/向量混合检索（RRF）+ RAG」** 的渐进式路线：

- **最大化复用**现有 SQLite 与 AI 引擎架构，改动集中在数据访问与检索层；
- **P0 阶段以最低风险**先跑通闭环，再逐步推进结构化分块与全量回填；
- 最终把知识库从「文本仓库」升级为可支撑智能问答的**知识资产**。

> 后续可基于本文档继续拆解为可执行的开发任务清单（Issue 级），或直接进入 P0 的 `sqlite-vec` 接入实现。
