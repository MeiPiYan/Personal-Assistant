# Personal Assistant 知识图谱气泡可视化规划书

> 文档版本：v1.0
> 编写日期：2026-09-19
> 适用项目：Personal Assistant（Windows 桌面端个人 AI 助手，Python + PySide6）
> 文档性质：交互规格定义 / 功能规划（**本文档只做规划，不含实现代码**）
> 前置依赖：《向量检索与知识库改造方案.md》P0–P2（已完成，提供向量索引与混合检索能力）

---

## 目录

1. [背景与目标](#1-背景与目标)
2. [核心概念定义](#2-核心概念定义)
3. [三级气泡体系规格](#3-三级气泡体系规格)
4. [交互行为规格](#4-交互行为规格)
5. [状态机与转换表](#5-状态机与转换表)
6. [1级气泡判定算法](#6-1级气泡判定算法)
7. [双击导航与联动](#7-双击导航与联动)
8. [视觉与动效设计](#8-视觉与动效设计)
9. [技术架构设计](#9-技术架构设计)
10. [配置项设计](#10-配置项设计)
11. [分阶段落地路线](#11-分阶段落地路线)
12. [测试与验收标准](#12-测试与验收标准)
13. [风险与应对](#13-风险与应对)
14. [待用户拍板的开放问题](#14-待用户拍板的开放问题)
15. [附录：关键代码位置索引](#15-附录关键代码位置索引)

---

## 1. 背景与目标

### 1.1 背景

项目已完成向量化改造（P0–P2）：知识条目、文档分块、日记均已生成 embedding 并写入 `vec_chunks`，对话检索通过混合检索（FTS5 + 向量 + RRF）完成。但当前知识库只有**列表式**呈现，数据之间已算出的语义关系（向量相似度）对用户完全不可见。

### 1.2 目标

把知识库从「列表」升级为「可探索的关系图谱」：

1. **气泡即知识点**：每个气泡代表一个知识点，气泡内标注关键词或短句；
2. **连线即关系**：半透明直线连接有关联的气泡，象征向量语义关系；
3. **选中即导航**：以"当前选中气泡"为中心，向关联知识逐级展开探索；
4. **双击即回溯**：双击气泡可直接跳入该知识点所属的存储面板，并居中高亮；
5. **检索即入口**：从对话引用 / 检索结果可一键进入图谱，以检索结果为原点。

### 1.3 非目标（本期不做）

- 不做全局全量图谱浏览（不一次性渲染全部知识点的"总览大图"，只做选中驱动的局部图谱）；
- 不做 3D 渲染；
- 不做多人协作 / 图谱分享；
- 逻辑关系（因果、引用）的 LLM 自动标注列为可选增强，非本期承诺项。

---

## 2. 核心概念定义

| 概念 | 定义 |
|---|---|
| 气泡（Node） | 一个知识点的可视化载体，圆形，内含关键词/短句标签 |
| 节点类型 | `chunk`（文档分块）、`knowledge_item`（知识条目）、`diary_entry`（日记，预留） |
| 连线（Edge） | 两个气泡间的半透明直线，代表向量语义关联 |
| 0级气泡 | **当前选中**的气泡，有且仅有一个，绿色 |
| -1级气泡 | **上一个0级**气泡（当前0级的"来处"），最多一个，不总是存在，红色 |
| 1级气泡 | 当前0级气泡的**关联下一级**，0~n 个，不总是存在，蓝色 |
| 选中（Select） | 使某气泡成为0级气泡的动作（悬停或单击触发） |
| 进入（Enter） | 双击气泡，跳转到该知识点所属存储面板 |

> **关键不变量（Invariant）**：任何时刻，图谱内 0级气泡恰好 1 个；-1级气泡 0 或 1 个；1级气泡 0 至 n 个（n 为配置上限）。该不变量是状态机实现与测试的核心。

---

## 3. 三级气泡体系规格

### 3.1 各级语义与数量约束

| 级别 | 语义 | 数量约束 | 颜色 | 大小（基准） |
|---|---|---|---|---|
| 0级 | 当前选中，探索焦点 | **恰好 1 个** | 绿色 | 最大（如 r=46） |
| -1级 | 上一个0级，回退路径提示 | **0 或 1 个** | 红色 | 中等（如 r=36） |
| 1级 | 当前0级的关联下一级 | **0 ~ n 个**（n 默认 8，可配置） | 蓝色 | 较小（如 r=30） |

### 3.2 何时各级存在

| 场景 | 0级 | -1级 | 1级 |
|---|---|---|---|
| 检索进入图谱 | 1 个（=检索结果） | **无** | 0~n 个（按向量关系判定） |
| 图谱内选中1级气泡 | 该1级气泡升级为新0级 | 旧0级降级为-1级 | 重新计算 0~n 个 |
| 连续探索（选中第 k 个1级） | 最新被选中的气泡 | 始终只有"上一步"的0级 | 重新计算 |
| 0级无任何关联知识 | 1 个 | 视路径而定 | **0 个（允许）** |

> 注意：-1级**不等于**"历史上所有访问过的气泡"，它永远只保留**最近的一步**。更早的访问路径如需保留，列为 P2 可选增强（面包屑），不在本期规格内。

### 3.3 布局规则

- 0级气泡**居中**于画布视口；
- 1级气泡环绕0级分布（圆周均布或按相似度大小映射半径：相似度越高越靠近圆心）；
- -1级气泡紧贴0级一侧，与0级之间的连线**加粗+提高不透明度**，暗示"来路"；
- 0级 ↔ 各1级之间画半透明连线（象征向量关系）；
- 1级与1级之间的横向连线默认不画（避免视觉噪音），列为可选配置项。

---

## 4. 交互行为规格

### 4.1 气泡上的鼠标行为

| 动作 | 目标气泡级别 | 行为 | 结果 |
|---|---|---|---|
| 悬停（hover，停留超过防抖阈值） | 1级 | 该1级气泡成为新0级 | 触发状态转换（见 §5），伴随布局动画 |
| 单击（click） | 1级 | 同上 | 同上（单击与悬停等价，保证触摸板/精确操作可用） |
| 悬停/单击 | 0级自身 | 无操作 | 状态不变（防止抖动循环） |
| 悬停/单击 | -1级 | -1级重新成为0级（**回退**） | 当前0级降级为-1级，支持"反悔回去" |
| 双击（double-click） | 任意级别气泡 | **进入**该知识点的存储面板 | 切换面板 + 定位 + 居中 + 高亮（见 §7） |
| 悬停（瞬时，低于防抖阈值） | 任意气泡 | 仅显示 tooltip（完整标签 + 来源） | 不触发级别转换 |

### 4.2 悬停防抖（Hover Debounce）

悬停即选中存在误触风险（鼠标划过路径上的气泡），因此引入防抖：

- **防抖阈值**：鼠标在气泡上停留 ≥ `hover_debounce_ms`（默认 250ms）才触发选中；
- 停留不足阈值即移开：只显示 tooltip，不转换；
- 连续快速扫过多个1级气泡：只有最终停留的气泡被选中，中间气泡不触发；
- 单击**不防抖**：点击是明确意图，立即生效。

### 4.3 画布上的其他交互

| 动作 | 行为 |
|---|---|
| 拖拽空白处 | 平移画布 |
| 滚轮 | 以鼠标位置为中心缩放（0.3x ~ 3x） |
| 拖拽气泡 | 临时移动该气泡位置（松手后是否吸附回布局位置，列为开放问题 Q5） |
| Esc | 清空选中态回到检索入口视图（若有检索上下文） |

---

## 5. 状态机与转换表

### 5.1 状态结构

```
GraphState = {
  current: NodeId,          # 0级气泡，必有
  previous: NodeId | None,  # -1级气泡，可空
  related: [NodeId, ...],   # 1级气泡集合，0~n
  history: [NodeId, ...]    # 内部访问栈（实现细节，支撑回退），对外不可见
}
```

### 5.2 转换表

| 编号 | 触发事件 | 前置状态 | 转换规则 | 后置状态 |
|---|---|---|---|---|
| T1 | 检索进入图谱 `enter_from_search(node)` | 任意 | current=检索结果；previous=None；related=KNN(node) | 1个0级，无-1级 |
| T2 | 选中1级气泡 `select(x)`，x ∈ related | 稳定态 | previous←current；current←x；related←KNN(x)（**剔除 previous，避免立即指回**） | 新0级+新-1级+新1级集 |
| T3 | 选中-1级气泡 `select(previous)`（回退） | previous≠None | new_current←previous；new_previous←current；related←KNN(new_current) | 与 T2 对称，支持反悔 |
| T4 | 选中0级自身 `select(current)` | 稳定态 | 无转换 | 状态不变 |
| T5 | 新0级无关联知识 | T2/T3 后 | related=[] | 允许1级为空，仅显示0级±-1级 |
| T6 | 双击任意气泡 `enter(x)` | 任意 | **不改变图谱状态**，仅触发面板跳转 | 图谱状态保持，便于返回继续探索 |

### 5.3 边界情况

| 边界 | 处理 |
|---|---|
| related 计算结果包含 previous | 剔除，防止选中1级后立即"指回来路"造成往返震荡；用户想回退可直接点红色-1级（T3） |
| related 计算结果包含 current 自身 | 剔除（KNN 查询天然会返回自身，必须过滤） |
| 同一气泡既是历史又是新关联 | 以新身份（1级）为准重新计算布局位置 |
| 知识条目无任何向量关联 | 1级集合为空，画布只显示居中的0级（±-1级），不报错 |
| 悬停与点击竞争 | 同一事件循环内点击优先；悬停计时器在 mouseRelease 时取消 |
| 快速连续切换（动画未结束再次选中） | 新请求打断当前动画，直接以新状态为目标重新布局（动画可中断） |

---

## 6. 1级气泡判定算法

### 6.1 数据来源

- 向量相似度：`vec_chunks`（sqlite-vec KNN 索引）或 `chunks.embedding` BLOB 的余弦相似度（兜底）；
- 该能力已由 P0 的 `VectorStore` 与 P2 的检索链路提供，**本期只需新增一个"按节点找邻居"的只读查询**，不改存储结构。

### 6.2 判定流程

```
输入：当前0级节点 node（含 embedding），配置 k（默认8）、相似度阈值 θ（默认 0.35）
1. 取 node 的 embedding
2. KNN 查询 top-(k+2)：优先走 sqlite-vec；不可用时全量余弦兜底
3. 过滤：剔除自身、剔除 previous、剔除相似度 < θ 的节点
4. 截断：按相似度降序取前 k 个
5. 输出：1级气泡集合（每个携带 score，用于布局半径与不透明度映射）
```

### 6.3 阈值与上限说明

- **θ（相似度下限）**：防止把"弱关联"也连成线。hashing 后端相似度分布与 bge 不同，θ 做成配置项，默认 0.35，允许用户在设置面板调节；
- **k（1级数量上限）**：默认 8。过多会造成视觉拥挤且违背"逐级探索"的设计意图；
- 排序：RRF 分数不参与本算法（图谱是纯向量关系视图）；如未来加"关键词共现边"，另行规划。

---

## 7. 双击导航与联动

### 7.1 跳转映射表

双击气泡 → 根据节点类型跳转到对应存储面板：

| 节点类型 | 目标面板 | 定位方式 | 高亮方式 |
|---|---|---|---|
| `knowledge_item` | 知识库面板 | 按 id 选中列表项并滚动到可见 | 该项背景闪烁/持续高亮，标题居中于可视区 |
| `chunk` | 文档面板 | 打开所属 `documents` 记录，滚动到对应分块文本 | 分块段落高亮 + 视口居中 |
| `diary_entry` | 日记面板（预留） | 按日期定位条目 | 条目高亮 |

### 7.2 面板侧需要的支撑能力（规划，不改码）

- 各存储面板需暴露统一的 `focus_item(kind, item_id)` 入口（新增接口，供图谱面板调用）；
- 高亮动效：目标项"居中到面板中间位置 + 高亮提醒"（如 2 秒呼吸高亮后渐隐为常驻选中态）；
- 图谱状态在跳转期间保留（T6），用户从存储面板切回图谱时可继续探索。

### 7.3 检索进入图谱的入口

| 入口 | 行为 |
|---|---|
| 对话面板 RAG 引用来源块 | 每个引用来源提供"在图谱中查看"入口 → 以该 chunk 为0级进入图谱（T1） |
| 知识库面板列表 | 条目右键/按钮"查看关系图谱" → T1 |
| 文档面板 | 文档详情内"查看知识图谱" → 以该文档首个分块为0级进入（T1） |

---

## 8. 视觉与动效设计

### 8.1 配色（已敲定）

| 级别 | 颜色 | 建议色值（亮/暗主题自适应） |
|---|---|---|
| 0级 | **绿色** | `#34A853` / 暗主题 `#4CC38A` |
| -1级 | **红色** | `#EA4335` / 暗主题 `#FF6B5E` |
| 1级 | **蓝色** | `#4285F4` / 暗主题 `#5B9BFF` |
| 连线 | 半透明灰 | 不透明度 25%–60%，按相似度映射（越相似越实） |

> 色值落入 `ThemeManager`（现有主题系统），保证明暗主题切换时协调。

### 8.2 气泡标签

- `knowledge_item`：直接用 `title`；
- `chunk`：无标题，需关键词抽取——离线 TF-IDF / RAKE-lite 抽取 1 个关键词或 ≤12 字短句（零依赖兜底）；LLM 生成短标签列为可选增强；
- 标签超长时省略号截断，完整内容放 tooltip。

### 8.3 动效

| 场景 | 动效 | 时长 |
|---|---|---|
| 级别转换（T2/T3） | 新0级平滑移动到中心；旧0级移动到-1位；新1级从0级周围"绽开" | ~300ms，可中断 |
| 新0级出现 | 一次脉冲高亮（缩放 1.0→1.15→1.0） | ~250ms |
| 连线出现 | 透明度从 0 渐入 | ~200ms |
| 双击跳转前 | 目标气泡轻微下压反馈 | ~100ms |

### 8.4 渲染技术

- 沿用 PySide6 自绘路线：**QGraphicsView + QGraphicsScene**（项目依赖为 PySide6-Essentials，无 QtWebEngine，不引入浏览器内核）；
- 气泡 = 自定义 `QGraphicsItem`（圆 + 文本 + 级别描边）；连线 = `QGraphicsLineItem`（半透明画笔）；
- 平移/缩放用视图原生能力；布局动画用 `QTimer` 驱动的插值。

---

## 9. 技术架构设计

### 9.1 新增模块（规划）

| 模块 | 职责 |
|---|---|
| `src/graph/__init__.py` | 包入口 |
| `src/graph/graph_state.py` | 三级状态机（§5），纯逻辑、无 UI 依赖，保证可单测 |
| `src/graph/graph_data.py` | GraphDataProvider：节点标签抽取 + 1级判定查询（§6），只读复用 DAO/VectorStore |
| `src/ui/graph_panel.py` | 图谱面板：QGraphicsView 画布、气泡/连线渲染、交互事件、动效 |
| `src/ui/widgets/graph_node.py` | 气泡 QGraphicsItem（级别配色、标签、tooltip、脉冲动画） |
| `src/ui/widgets/graph_edge.py` | 连线 QGraphicsItem（半透明、相似度映射不透明度） |

### 9.2 与现有系统的接口

- `VectorStore` / DAO：提供 embedding 与节点元数据（**只读**，不改表结构）；
- `main_window`：侧边栏新增"图谱"按钮 + QStackedWidget 注册 `graph_panel`；提供 `show_graph(node_ref)` 供各入口调用（T1）；
- 各存储面板：新增 `focus_item()` 接口（§7.2）；
- `ThemeManager`：注册图谱面板，接入明暗主题；
- `Config`：新增 `ai.graph.*` 配置段（§10）。

### 9.3 关键设计取舍

| 取舍点 | 决策 | 理由 |
|---|---|---|
| 全局大图 vs 选中驱动局部图 | **选中驱动**（只渲染 0/-1/1 级邻域） | 节点规模可控（≤10个），布局与性能零风险，且契合"逐级探索"交互 |
| 悬停即选中 vs 仅点击选中 | **都支持，悬停加防抖** | 用户已敲定悬停/点击等价；防抖规避误触 |
| 状态机独立于 UI | **独立纯逻辑模块** | 不变量可单测，UI 崩溃不污染状态 |
| 每步实时算邻居 vs 预计算 | **实时 KNN** | 数据量小（k≤8），vec0 查询毫秒级；无需预计算表 |

---

## 10. 配置项设计

新增 `config/settings.yaml` 配置段：

```yaml
ai:
  graph:
    enabled: true              # 图谱面板总开关
    level1_top_k: 8            # 1级气泡数量上限
    similarity_threshold: 0.35 # 1级判定相似度下限 θ
    hover_debounce_ms: 250     # 悬停选中防抖阈值
    anim_duration_ms: 300      # 级别转换动画时长
    draw_lateral_edges: false  # 是否绘制 1级-1级 横向连线
    label_max_chars: 12        # 气泡标签最大字符数
```

---

## 11. 分阶段落地路线

### G-P0：状态机 + 静态图谱 MVP

- `graph_state.py`：三级状态机 + 不变量 + 转换表（T1–T6）全部实现；
- `graph_data.py`：1级判定查询（KNN + 阈值过滤）+ 关键词标签抽取（离线 TF-IDF/RAKE 兜底）；
- `graph_panel.py`：QGraphicsView 静态渲染（0级居中、1级环绕、-1级侧置、半透明连线）；
- 交互：**仅单击**选中（T2/T3/T4），双击跳转映射（T6 + 各面板 `focus_item`）；
- 测试：状态机单测（不变量/转换/边界）+ 数据层单测（stub store）。

### G-P1：悬停选中 + 动效 + 检索入口

- 悬停防抖选中（§4.2）+ tooltip；
- 级别转换/脉冲/连线渐入动效（§8.3）；
- 对话引用来源、知识库面板、文档面板的"在图谱中查看"入口（§7.3）；
- 配置段 `ai.graph.*` 接入设置面板；
- 测试：防抖逻辑单测 + UI 级离屏渲染冒烟测试。

### G-P2：体验增强（可选）

- 画布平移/缩放打磨、气泡拖拽、访问面包屑（历史路径）；
- LLM 生成气泡短标签 / 边关系语义标注（可选增强）；
- 视图导出截图；
- 日记节点接入图谱。

---

## 12. 测试与验收标准

### 12.1 测试策略

| 层 | 内容 | 方式 |
|---|---|---|
| 状态机 | 不变量（恰好1个0级；≤1个-1级）、T1–T6 转换、边界（空关联/自环/指回来路剔除/快速连击） | pytest 纯逻辑单测 |
| 数据层 | 1级判定：KNN 结果过滤自身/previous/低分、截断 top-k、空库不报错 | stub VectorStore/DAO 单测 |
| 标签抽取 | 中文关键词抽取非空、长度截断、空文本兜底 | 单测 |
| UI | 面板创建、气泡级别配色、双击事件派发 | 离屏 QGraphicsScene 冒烟测试 |

### 12.2 验收标准（G-P0 为例）

1. 检索进入图谱：恰好 1 个绿色0级 = 检索结果，无红色-1级，蓝色1级 0~8 个；
2. 单击任一蓝色1级：它变为绿色0级居中，原0级变红色-1级，出现新蓝色1级集；
3. 单击红色-1级：回到上一个0级（回退成功）；
4. 双击任意气泡：跳转到对应存储面板，目标项居中并高亮；
5. 关联为空的知识点：只显示0级，不崩溃；
6. 全量测试套件不回归。

---

## 13. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 悬停即选中导致误触/闪烁 | 体验差 | 250ms 防抖 + 点击优先 + 动画可中断 |
| hashing 后端相似度分布偏平，θ 难调 | 1级过多/过少 | θ 配置化；设置面板暴露滑杆；后续换 bge 后提供推荐值 |
| chunk 无标题，标签质量差 | 气泡不可读 | TF-IDF/RAKE 离线兜底；LLM 短标签列为可选增强 |
| 双击定位需要各面板配合改造 | 跨模块改动 | 统一 `focus_item()` 接口，逐个面板增量接入 |
| 挂载目录 I/O 抖动（既有环境问题） | 开发/测试中断 | 延续既有策略：e2b_read/write 落盘 + 沙箱 `git archive` 导出运行测试 |

---

## 14. 待用户拍板的开放问题

| 编号 | 问题 | 选项 | 建议 |
|---|---|---|---|
| Q1 | 悬停防抖时长 | 150 / 250 / 400 ms | 默认 250ms，可在设置中调 |
| Q2 | 1级气泡数量上限 k | 5 / 8 / 12 | 默认 8 |
| Q3 | 相似度阈值 θ 默认值 | 0.25 / 0.35 / 0.45 | 默认 0.35（hashing 后端下先偏严） |
| Q4 | -1级是否参与1级集的关联判定 | 仅剔除 vs 保留但置灰 | 建议仅剔除（规格已按此写） |
| Q5 | 拖拽气泡松手后 | 吸附回布局位 vs 停在拖放处 | 建议吸附回（保持以0级为中心的几何语义） |
| Q6 | 画布是否需要"无关背景气泡" | 只渲染三级 vs 加灰显背景节点 | 建议只渲染三级（本期选中驱动定位） |
| Q7 | 日记节点是否纳入首期 | 纳入 vs 预留 | 建议预留（G-P2 再接） |

---

## 15. 附录：关键代码位置索引

| 能力 | 位置 |
|---|---|
| 向量查询（KNN / 余弦兜底） | `src/search/vector_search.py` → `VectorStore.search()` |
| 混合检索结果（图谱入口数据） | `src/search/hybrid_search.py` → `HybridSearcher.search()` |
| 数据表（chunks/vec_chunks/knowledge_items） | `src/storage/database.py` |
| 数据访问 | `src/storage/dao.py` |
| 面板注册 / 栈切换 | `src/ui/main_window.py` |
| 知识库面板（focus_item 接入点） | `src/ui/knowledge_panel.py` |
| 文档面板（focus_item 接入点） | `src/ui/document_panel.py` |
| 对话引用来源（图谱入口） | `src/ui/chat_panel.py` |
| 主题系统 | `src/ui/styles.py` → `ThemeManager` |
| 配置 | `src/app/config.py` + `config/settings.yaml` |
